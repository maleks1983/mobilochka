import { NavMenuHoverDirective } from './nav-menu-hover.directive';

describe('NavMenuHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NavMenuHoverDirective();
    expect(directive).toBeTruthy();
  });
});
